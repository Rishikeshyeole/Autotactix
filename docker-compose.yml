version: '3.8'

services:
  backend:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env

  # Apply the same rule to any additional containers:
  # novnc:
  #   image: ...
  #   restart: unless-stopped

